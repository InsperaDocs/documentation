---
title: "Document Status Check"
description: "Check the status of an uploaded document."
api: "GET /"
---

This endpoint retrieves the current status of a document identified by `documentId`. The `documentId` must be provided as a path parameter, immediately following `/document/` and preceding `/checkstatus`.

<ParamField path="document" type="string" required>
  Enter `document` as a path parameter.
</ParamField>

<ParamField path="documentId" type="string" required={false}>
  Enter `documentId` as a path parameter.
</ParamField>

<ParamField path="checkStatus" type="string" required>
  Enter `checkStatus` as a path parameter.
</ParamField>