---
title: 'Institution Info (Page & Limit)'
description: 'Retrieve institution information with pagination and limit.'
api: "GET /institution-info"
---

This GET request is used to retrieve information about an institution, including its faculties, departments, and how they are related.

Once you've entered the Bearer token for authentication, simply append the `/institution-info` path to the base URL. This will return a structured response containing the institution’s details, including how faculties and departments are organized and connected.